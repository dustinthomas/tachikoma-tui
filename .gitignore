# Local environment / secrets
.local
.env.local
.env
*.token
gh_token
github_token

# IDE / editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Julia
Manifest.toml
.julia/

# Build artifacts (if any)
dist/
/build/
# docs/build/ is committed so `O` / open_local_docs works after git pull
# without requiring Documenter on every machine. Rebuild + re-commit when
# docs/src or docs/make.jl change: julia --project=docs docs/make.jl
docs/Manifest.toml
package-lock.json

# Tooling data (never commit)
.ai-metrics/
.grok/sessions/
.grok/cache/

# Project specific
# Add your generated data, large models, etc.


# Agentic / Grok session artifacts (do not commit)
agent_logs/
implementer/
*.tach
LocalPreferences.toml

# Julia coverage and build noise
*.cov
*.mem

