# API Reference

Stub page for Documenter `@autodocs` / manual API pages.

Full public-API documentation is a later gate (PR11). Until then, see:

- Exported names in `src/TachikomaTUI.jl`
- Docstrings on public functions/types (added as features land)
- [SPC Workbench](@ref) for app-level workflow notes

```@meta
CurrentModule = TachikomaTUI
```

!!! note "Scaffold"
    `@autodocs` blocks will be enabled when the docs build is required green (PR11).
